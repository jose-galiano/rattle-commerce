
<div id="{{ page.title |  replace: " ", "_" }}" class="container-fluid">
  <h1>
    {{ page.title }}
  </h1>

  <div>
    {% include 'page-multi-column', content: page.content %}
  </div>
</div>

<style>
  #{{ page.title |  replace: " ", "_" }}{
  overflow:hidden;
  }
</style>