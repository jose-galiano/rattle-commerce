$(document).ready(function () {
 //   $('.fulltext').hide();

    $('.readmore').click(function (event) {
        event.preventDefault();
        var description = document.querySelector('.product-description');
        console.log(description.style.height + ' Status check Passed')
        console.log(description.style.animation + 'Status check Passed')
        
        if (description.style.height == '0px'){
		  description.style.animation = 'open-desc .8s linear 0s 1 normal forwards';
          setTimeout(function(){ description.style.height = 'auto'}, 1000); 
          console.log(' Status check Passed')
          
        } else if (description.style.height == 'auto'){
          console.log('Status check Passed')
        description.style.height = '600px'  
        description.style.animation = 'close-desc .8s linear 0s 1 normal forwards';
         setTimeout(function(){ description.style.height = '0'}, 1000);
        
        
        }else{
          
        description.style.height = '0px'  
        console.log(' else')
        }

          
          setTimeout(function(){   console.log('Mode ' + description.style.height)}, 1000);
          console.log('Computing styling ' + description.style.animation)
          
           setTimeout(function(){   console.log('Status check Passed ')}, 1300);
          setTimeout(function(){   console.log('Execution Finalized')}, 1550);
          
          $(this).text($(this).text() == 'See less ▲' ? 'Compare ▼ ' : 'See less ▲');
    });
});

$(window).resize(function () {
    $('.slick-slide').not('.slick-initialized').slick('resize');
});

$(window).on('orientationchange', function () {
    $('.slick-slide').not('.slick-initialized').slick('resize');
});

$(document).ready(function(){
$('.desktop_slide').slick({
infinite: true,
slidesToShow: 4,
slidesToScroll: 1,
autoplay: true,
arrows: false,
autoplaySpeed: 5000,
pauseOnHover:true
});
});

$(document).ready(function(){
$('.mobile_slide').slick({
infinite: true,
slidesToShow: 2,
slidesToScroll: 1,
autoplay: true,
arrows: false,
autoplaySpeed: 4000,
pauseOnHover:true
});
});

$(function () {
  $(document).scroll(function () {
    var $nav = $(".navbar-darks");
    $nav.toggleClass('scrolled', $(this).scrollTop() > $nav.height());
  });
});
$(function () {
  $(document).scroll(function () {
    var $nav = $(".mob_navb");
    $nav.toggleClass('scrolled', $(this).scrollTop() > $nav.height());
  });
});

$('.dropdown').hover(function(){ 
  $('.dropdown-toggle', this).trigger('click'); 
});


$(document).ready(function(){
    $(".dropdown").hover(function(){
        var dropdownMenu = $(this).children(".dropdown-menu");
        if(dropdownMenu.is(":visible")){
            dropdownMenu.parent().toggleClass("open");
        }
    });
});  

$(document).ready(function(){
$('.featured_in').slick({
infinite: true,
slidesToShow: 4,
slidesToScroll: 1,
autoplay: true,
arrows: false,
autoplaySpeed: 3000,
pauseOnHover:false
  
      });
    });

$(document).ready(function(){
$('.testimonial_by').slick({
infinite: true,
slidesToShow: 1,
slidesToScroll: 1,
autoplay: true,
arrows: false,
autoplaySpeed: 3100,
pauseOnHover:true
  
      });
    });


$(document).ready(function(){
 $('.product_imgs').slick({
  dots: false,
  infinite: true,
  fade: true,
  cssEase: 'linear',
  autoplay: true,
  arrows: false,
  autoplaySpeed: 3100, 
  slidesToShow: 1,
  slidesToScroll: 1,
  asNavFor: '.product_imgs_nav',
  pauseOnHover:true
});
$('.product_imgs_nav').slick({
  slidesToShow: 5,
  infinite: true,
  slidesToScroll: 1,
  asNavFor: '.product_imgs',
  dots: false,
  arrows: false,
  focusOnSelect: true
});
   });



var x = screen.width;
  if (x >= 992) {
  console.log("Desktop");
  } else {
  console.log("Mobile");
  };

$(document).ready(function(){
$('.testimonial_prod').slick({
infinite: true,
slidesToShow: 1,
slidesToScroll: 1,
autoplay: true,
  dots: true ,
arrows:false,
autoplaySpeed: 3000,
pauseOnHover:true
 
      });
    });

$(document).ready(function(){
$('.news_car').slick({
infinite: true,
slidesToShow: 2,
slidesToScroll: 1,
autoplay: true,
  dots: false ,
arrows:false,
autoplaySpeed: 2300,
pauseOnHover:false
 
      });
   
    });

var globalAccordions = {
  init: function(){
    var flg = 0;
    var $faqHeading = $('.faqAccordion > dt > button, .accordion > dt > a');
    $('.faqAccordion > dd, .accordion > dd').attr('aria-hidden',true);
    $faqHeading.attr('aria-expanded',false);
    $faqHeading.on('click activate',function(){
      if( flg ) return false;
      flg = 1;
      var state = $(this).attr('aria-expanded') === 'false' ? true : false;
      $(this).attr('aria-expanded',state);
      $(this).parent().next().slideToggle(function(){
        flg = 0;
      });
        $(this).parent().next().attr('aria-hidden',!state);
      return false;
    });
    $faqHeading.on('keydown',function(event){
      var keyCode = event.keyCode || e.which;
      if (keyCode === 13){
        $(this).trigger('activate');
      }
    });
  }
};



$(document).ready(function(){
$('.process_time').slick({
infinite: true,
slidesToShow: 1,
slidesToScroll: 1,
autoplay: true,
  dots: true ,
arrows:false,
autoplaySpeed: 3000,
pauseOnHover:true
 
      });
    });


$(document).ready(function(){

const registerVideo = (bound, video) => {
	bound = document.querySelector(bound);

	video = document.querySelector(video);
  if (video == null){return; };
  
  const scrollVideo = ()=>{
		if(video.duration) {
			const distanceFromTop = window.scrollY + bound.getBoundingClientRect().top;
			const rawPercentScrolled = (window.scrollY - distanceFromTop) / (bound.scrollHeight - window.innerHeight);
			const percentScrolled = Math.min(Math.max(rawPercentScrolled, 0), 1);
			
			video.currentTime = video.duration * percentScrolled;
		}
		requestAnimationFrame(scrollVideo);
	};
	requestAnimationFrame(scrollVideo);

};

registerVideo("#bound-one", "#bound-one video");
registerVideo("#bound-two", "#bound-two video");
              
  });




 

$(document).ready(function(){
$('.homepageslide').slick({
infinite: true,
slidesToShow: 1,
slidesToScroll: 1,
autoplay: true,
dots: true ,
arrows:false,
autoplaySpeed: 3000,
pauseOnHover:true
 
      });
    });

$(document).ready(function(){
$('.testis').slick({
  slidesToShow: 2,
  autoplay: true,
  dots: true ,
  autoplaySpeed: 5000,
  pauseOnHover:true,
  infinite: true,
  responsive: [
    {
      breakpoint: 768,
      settings: {
        arrows: false,
        centerMode: true,
        centerPadding: '40px',
        slidesToShow: 3
      }
    },
    {
      breakpoint: 480,
      settings: {
        arrows: false,
        centerMode: true,
        centerPadding: '40px',
        slidesToShow: 1
      }
    }
  ]
});
    });

	$(document).ready(function() {
			$('.minus').click(function () {
				var $input = $(this).parent().find('input');
				var count = parseInt($input.val()) - 1;
				count = count < 1 ? 1 : count;
				$input.val(count);
				$input.change();
				return false;
			});
			$('.plus').click(function () {
				var $input = $(this).parent().find('input');
				$input.val(parseInt($input.val()) + 1);
				$input.change();
				return false;
			});
		});